---
:hipchat_api_token: '<%= scope.lookupvar('puppet_hipchat::api_token') %>'
:hipchat_from: '<%= scope.lookupvar('puppet_hipchat::from') %>'
:hipchat_room: '<%= scope.lookupvar('puppet_hipchat::room') %>'
:hipchat_notify: '<%= scope.lookupvar('puppet_hipchat::notify') %>'
:hipchat_statuses:
<% scope.lookupvar('puppet_hipchat::statuses').each do |status| -%>
  - <%= status %>
<% end -%>
:foreman_api_host: '<%= scope.lookupvar('puppet_hipchat::foreman_api_host') %>'
